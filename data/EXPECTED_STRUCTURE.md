
# Expected local data structure

```text
<DATA_ROOT>/
├── CICIDS2017/
│   └── [provider files]
└── CICIoT2023/
    └── [provider files]

<PROJECT_ROOT>/
├── outputs_step6_data_integrity_v2/
├── cache_r4_revision/
├── assignments_r4_revision/
└── outputs_r4_revision/
```

The exact provider-file discovery rules are documented in the locked R1 data-flow files and the
executed configuration. Run preflight before expensive computation.
